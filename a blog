
真是个咸鱼啊
433要开始接着做了 两个月没动了
然后也开始在oj上做点题 从简单的开始做
今天ac了7道 —— 3.9
========================================
//  计算一行整数中所有奇数的乘积，注意格式
#include<stdio.h>
int main()
{
	int m, n, sum;
	while( scanf("%d",&n) != EOF ){
		sum = 1;
		while(n--){
			scanf("%d",&m);
			if( m%2 == 1 )
				sum*=m;
		}
		printf("%d\n",sum);
	}
	return 0;
}
=========================================
//  给定一段连续的整数，求出他们中所有
偶数的平方和以及所有奇数的立方和。踩了个坑，
计算之前要记得判断两个数的大小，如果不判断
就直接计算可能会出错。
#include<stdio.h>
int main()
{
	int m,n,sum1,sum2,t;
	while( scanf("%d %d",&m,&n) != EOF ){
		sum1 = sum2 = 0;
		if(m > n){
			t = m;
			m = n;
			n = t;
		}
		while(m <= n){
			if(m%2 == 0)
				sum1 += m*m;
			else
				sum2 += m*m*m;
			m++;
		}
		printf("%d %d\n",sum1,sum2);
	}
	
	return 0;
}
																--3.15
																
   跳坑日记...这两天在做433数据展示界面，然后显示wcy卡在了顶部TabLayout切换，然后帮着找到了问题。然后我今天做积分榜，不是很难做，只需要把
首页的那个recycleView稍微改装一下就可以了，但是又出现了闪退的问题，然后上网查，说有可能是Fragment中用的getActivity()返回了空指针，然后博
文里给了两种解决方法，但是发现都不能解决我的问题，然后在运行日志里面发现了一个错误，显示的是RankItemAdapter调用和设置text时出现了错误，上
网查找到了原因————
   “在setText（）方法中设置了一个int型变量，在运行的时候就会报String resource id #0x0的错误，解决方法是把int型数据改成String类型，如
setText(5+""); 因为这样Android系统就会主动去资源文件当中寻找， 但是它不是一个资源文件ID， 所以就会报出这个bug。”
   然后把几个int型改回了string，就可以正常运行了。还有就是看到了一篇博文，里面讲Fragment讲得很深很细，有讲到之前上学期遇到的那个问题，几个
Fragment重叠显示，打算以后再仔细看看那篇博文，有必要好好理解一下Fragment和Activity的生命周期。
                                                                                                                                --3.22	


